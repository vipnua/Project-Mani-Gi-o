import mongoose from "mongoose";
const PostMetaSchema = new mongoose.Schema({
    Post_id: { type: Number, required: true },//The post id to identify the parent post.
    Key: { type: String, required: true },//The key identifying the meta.
    Content:{type:String,default:null},
})
export default mongoose.model('PostMeta',PostMetaSchema);